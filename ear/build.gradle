plugins {
    id 'ear'
}

dependencies {
    deploy project(path: ':ejbs', configuration: 'archives')
    deploy project(path: ':web', configuration: 'archives')
}
    
ear {
    archivesBaseName = "application"
}
